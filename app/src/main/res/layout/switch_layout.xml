<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/switchView"
    android:layout_width="200dp"
    android:layout_gravity="right"
    android:gravity="center_horizontal"
    android:layout_height="match_parent"
    android:orientation="horizontal">


    <TextView
        android:id="@+id/tvSwitchMode"
        android:gravity="center"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="20dp"
        android:layout_marginTop="5dp"
        android:layout_marginBottom="5dp"
        android:textColor="#fff"
        android:background="@drawable/rounded_corner"
        android:fontFamily="sans-serif"
        android:textSize="9sp"
        android:text="Pozovi\n me"/>

  <!--
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pošalji\n dopunu"
        android:singleLine="false"
        android:minLines="2"
        android:textSize="9sp"
        android:textColor="#fff"
        android:id="@+id/textView"
        android:layout_alignBottom="@+id/switchForActionBar"
        android:layout_toLeftOf="@+id/switchForActionBar"
        android:layout_toStartOf="@+id/switchForActionBar"
        android:layout_alignTop="@+id/switchForActionBar" />

    <android.support.v7.widget.SwitchCompat
        android:id="@+id/switchForActionBar"
        android:layout_width="50dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text=""
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pozovi\n me"
        android:layout_marginLeft="3dp"
        android:singleLine="false"
        android:gravity="center"
        android:textSize="9sp"
        android:textColor="#fff"
        android:layout_alignBottom="@+id/switchForActionBar"
        android:layout_toRightOf="@+id/switchForActionBar"
        android:layout_toEndOf="@+id/switchForActionBar"
        android:id="@+id/textView3"
        android:layout_alignTop="@+id/switchForActionBar" />
    -->

</RelativeLayout>